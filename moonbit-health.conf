# Release policy used by the command-line tool and CI.
profile = release
minimum_score = 90
maximum_errors = 0
maximum_warnings = 1
maximum_infos = 2
require_ci = true
require_tests = true
require_changelog = true
